เรื่อง control flow
การใช้ switch --> ใช้ในการตรวจสอบเงื่อนไข
switch (expression) {
    case const-expr:
        statements
    case const-expr:
        statements
    default: --> จะมีก็ได้ไม่มีก็ได้
    statements
}
การใช้ลูป while
    expr1;
    while (expr2) {    
        statement
        expr3;
    }
    ซึ่งสามารถใช้ในfor ได้เช่น
        for (expr1; expr2; expr3)
            statement 
            ซึ่งขะลดบรรทัดลงจากลูป while
            และถ้าไม่มี expr สามารถทำแบบนี้ได้ --> for (;;) {
การใช้ลูป do while
    do{
        stament
    while (expression); --> ะทำงานเรื่อยๆจนกว่า expression จะมีค่าความจริงเป็นเท็จ
    }
 break และ continue --> จะช่สวยการควบคุมการทำงานมากขึ้น
  ส่วน break ช่วยการหลุดออกจาก ลูปต่างๆ
